###############################################################
######  		WINDOWS BUILD ENVIRONMENT				#######
###############################################################

ifeq ($(XWSROOT),)
    $(error xmake: XWSROOT is not defined)
endif

ifneq ($(XOS),Linux)
	$(error Current OS is not Linux)
endif

ifeq ($(TGTPLATFORM),)
    TGTPLATFORM=linux
endif
